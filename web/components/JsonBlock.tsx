'use client';

// Backwards-compatible alias. Existing call sites get the JSON/YAML toggle
// for free via CodeBlock; new code should prefer CodeBlock directly.
import CodeBlock from './CodeBlock';

export default CodeBlock;
