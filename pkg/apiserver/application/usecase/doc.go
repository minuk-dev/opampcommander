// Package usecase gathers the application-layer use case interfaces (the
// inbound ports the primary adapters depend on). Supporting types such as
// ListOptions, GetOptions and the transaction runner remain in the sibling
// application/port package.
package usecase
